# 定位

## 获取定位服务

> 添加：[:octicons-tag-24: Version 1.5.2](https://sakurajimamaii.github.io/AVE-DOC/version/tools/#152)

```kotlin
val manager = getLocationManager()
```

## 判断定位是否可用

> 添加：[:octicons-tag-24: Version 1.5.2](https://sakurajimamaii.github.io/AVE-DOC/version/tools/#152)

`isLocationEnabled` 用于判断定位是否可用。

```kotlin
val enable = isLocationEnabled()
```
